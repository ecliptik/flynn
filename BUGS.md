# Bugs

List of bugs in flynn after user testing

## Keybaord
- ESC doesn't work in vi, is always stuck in INSERT mode and cannot go to command or visual mode with real keyboard via snow
- ctrl-c, ctrl-d, ctrl-e, etc don't work at a bash shell with a real keyboard via snow
- ctrl-x (or other keys don't work in nano) via snow

Maybe have an option to use Opt and Mac keys as esc and ctrl if the M0110 keyboard is the only supported keybaord in 6.0.8?
